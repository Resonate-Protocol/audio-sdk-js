{
  "type": "module",
  "name": "improv-audio",
  "version": "1.0.0",
  "description": "Play audio from JavaScript",
  "scripts": {
    "build": "tsc",
    "serve": "serve -l 3000 .",
    "dev": "concurrently \"tsc --watch\" \"serve -l 3000 .\"",
    "clean": "rimraf dist",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serve ."
  },
  "author": "Open Home Foundation",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/ws": "^8.18.0",
    "concurrently": "^8.2.0",
    "rimraf": "^5.0.0",
    "serve": "^14.2.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ws": "^8.18.1"
  }
}
